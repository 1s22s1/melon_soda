# bit = 0
N = 3

for bit = 0:(1<<N)-1
    @show bit
end

# n = 3

# for (int bit = 0; bit < (1<<n); ++bit)
#     {
#         /* bit で表される集合の処理を書く */

#     }

# bit = 0

# for bit = bit < (1<<n)
# end
